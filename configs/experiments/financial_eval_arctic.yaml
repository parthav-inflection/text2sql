experiment_name: financial_eval_arctic
dataset:
  name: financial
  subset_size: null
  data_dir: ./data/financial

evaluation:
  metrics:
    - deepeval_correctness
  batch_size: 1

output:
  results_dir: ./results
  save_predictions: true
  save_execution_results: true

agents:
  - name: "Arctic-Text2SQL-ToolCalling"
    agent_config_path: "configs/agents/standard_agent.yaml"
    model_config_path: "configs/models/arctic_text2sql.yaml" 