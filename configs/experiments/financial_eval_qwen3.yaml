experiment_name: financial_eval_qwen3
dataset:
  name: financial
  subset_size: null
  data_dir: ./data/financial

evaluation:
  metrics:
    - deepeval_correctness
  batch_size: 1

output:
  results_dir: ./results
  save_predictions: true
  save_execution_results: true

agents:
  - name: "Qwen3-ToolCalling"
    agent_config_path: "configs/agents/standard_agent.yaml"
    model_config_path: "configs/models/qwen3_8b.yaml" 