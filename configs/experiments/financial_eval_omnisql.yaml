experiment_name: financial_eval_omnisql
dataset:
  name: financial
  subset_size: null
  data_dir: ./data/financial

evaluation:
  metrics:
    - deepeval_correctness
  batch_size: 1

output:
  results_dir: ./results
  save_predictions: true
  save_execution_results: true

models:
  - configs/models/omnisql.yaml